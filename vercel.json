{
  "version": 2,
  "buildCommand": "cd color_changing_nft_frontend && npm install && npm run build",
  "outputDirectory": "color_changing_nft_frontend/build",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/.*", "dest": "/index.html" }
  ],
  "github": {
    "silent": true
  }
}
